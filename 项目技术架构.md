# 项目技术架构文档

## 1. 整体架构概述

### 1.1 架构设计
本项目采用经典的三层架构设计：

```
┌─────────────────────────┐
│     前端静态页面         │ index.html / post_detail.html / user_favorites.html ...
└───────────────▲─────────┘
                │ HTTP
┌───────────────┴─────────┐
│   后端服务（Django+DRF） │ 认证 / 贴吧 / 帖子 / 评论 / 收藏 API
└───────────────▲─────────┘
                │ ORM
┌───────────────┴─────────┐
│       数据库（SQLite）    │ user / tieba / post / comment / favorite
└─────────────────────────┘
```

### 1.2 技术栈选择

- **前端**：
  - HTML5 + CSS3
  - JavaScript (原生)
  - AJAX 异步交互
  - Django 模板系统

- **后端**：
  - Python 3.x
  - Django 5.2.7
  - Django REST Framework (DRF)
  - 认证：SessionAuthentication / BasicAuthentication

- **数据库**：
  - SQLite (开发环境)
  - 支持切换到 MySQL (生产环境)

- **工具与插件**：
  - corsheaders (跨域支持)
  - django.contrib.auth (用户认证)

## 2. 模块架构

### 2.1 用户模块
- **功能**：注册、登录、注销、权限控制
- **模型**：Django内置User + 自定义扩展
- **路由**：`/login/`, `/logout/`, `/register/`
- **视图**：LoginView, RegisterView, LogoutView

### 2.2 分类模块
- **功能**：管理贴吧分类
- **模型**：Category(name, slug, created_at)
- **路由**：`/api/categories`
- **视图**：CategoryViewSet

### 2.3 贴吧模块
- **功能**：贴吧列表、详情、排序
- **模型**：Bar(name, description, cover_url, followers, category, created_at)
- **路由**：`/bars/<bar_id>/`, `/api/bars`
- **视图**：bar_detail, BarViewSet

### 2.4 帖子模块
- **功能**：发帖、浏览、排序、评论
- **模型**：Post(title, content, author, bar, created_at, updated_at, last_reply_at)
- **路由**：`/posts/<post_id>/`, `/posts/new/`, `/api/posts`
- **视图**：post_detail, create_post, PostViewSet

### 2.5 评论模块
- **功能**：评论、楼中楼回复、点赞
- **模型**：Comment(post, author, content, likes, parent, created_at)
- **路由**：集成在帖子详情
- **视图**：评论创建与展示集成在post_detail中

### 2.6 收藏模块
- **功能**：帖子收藏、收藏列表、取消收藏
- **模型**：Favorite(user, post, created_at)
- **路由**：`/user/favorites/`, 帖子收藏功能集成在帖子详情
- **视图**：user_favorites, 收藏/取消收藏API

## 3. 数据模型设计

### 3.1 核心模型关系

```
User (用户)
  ├── 1:N ──► Post (帖子)
  ├── 1:N ──► Comment (评论)
  └── 1:N ──► Favorite (收藏)

Category (分类)
  └── 1:N ──► Bar (贴吧)

Bar (贴吧)
  └── 1:N ──► Post (帖子)

Post (帖子)
  ├── 1:N ──► Comment (评论)
  └── 1:N ──► Favorite (收藏)

Comment (评论)
  └── N:1 ──► Comment (回复，自关联)
```

### 3.2 收藏功能数据模型

```python
class Favorite(models.Model):
    user = models.ForeignKey(User, related_name='favorites', on_delete=models.CASCADE)
    post = models.ForeignKey(Post, related_name='favorites', on_delete=models.CASCADE)
    created_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        unique_together = ('user', 'post')
        
    def __str__(self):
        return f"{self.user.username} 收藏了 {self.post.title}"
```

## 4. 关键技术实现

### 4.1 认证与权限
- 基于Django内置的认证系统
- 使用会话认证与会话Cookie管理登录状态
- 权限控制确保未登录用户无法访问受保护页面

### 4.2 API设计
- RESTful风格API
- 支持GET/POST/PUT/DELETE方法
- 分页机制优化列表查询性能

### 4.3 异步交互
- AJAX实现无刷新操作（如收藏/取消收藏）
- JSON数据交换格式
- 错误处理与状态反馈

### 4.4 性能优化
- 数据库索引优化查询
- 预加载关联对象减少N+1查询
- 分页加载大型数据集

## 5. 部署与集成

### 5.1 开发环境
- 前端：静态文件直接预览
- 后端：`python manage.py runserver 127.0.0.1:8000`
- 静态预览：`python -m http.server 8010`

### 5.2 生产环境（建议）
- Web服务器：Nginx
- 应用服务器：Gunicorn/uWSGI
- 数据库：MySQL
- 缓存：Redis
- 安全：HTTPS、CSRF保护、XSS防护

## 6. 扩展性考虑

### 6.1 功能扩展
- 搜索功能增强（Elasticsearch）
- 实时通知系统
- 富文本编辑器
- 文件上传与管理

### 6.2 性能扩展
- CDN加速静态资源
- 读写分离
- 分布式缓存
- 水平扩展应用服务器

### 6.3 安全扩展
- JWT认证替代会话认证
- 多因素认证
- 内容审核系统
- 防爬虫与限流机制