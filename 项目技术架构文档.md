# 项目技术架构文档

## 1. 架构总览
- 分层结构：前端静态展示层 → 后端服务层（Django + DRF） → 数据存储层（SQLite/可切换 MySQL）
- 开发预览：`http://127.0.0.1:8010/index.html`（静态）与 `http://127.0.0.1:8000/`（后端）并行联调
- 目标：保证核心页面与接口通路打通，后续可平滑升级到组件化前端与生产级部署

```
┌─────────────────────────┐
│     前端静态页面         │ index.html / post_detail.html ...
└───────────────▲─────────┘
                │ HTTP
┌───────────────┴─────────┐
│   后端服务（Django+DRF） │ 认证 / 贴吧 / 帖子 / 评论 API
└───────────────▲─────────┘
                │ ORM
┌───────────────┴─────────┐
│   数据库（SQLite / MySQL）│ user / tieba / post / comment
└─────────────────────────┘
```

## 2. 技术栈与版本
- `Django 5.2.7`（项目骨架，视图/模板/管理命令）
- `Django REST Framework`（接口层，ViewSet + Router）
- `corsheaders`（跨域支持）
- 前端：静态 `HTML/CSS`（可渐进引入 Vue/React/Veaury）
- 数据库：默认 `SQLite`，生产可切换 `MySQL 8.0`

## 3. 后端架构
**应用注册**（`tieba_backend/tieba_backend/settings.py`）
- `INSTALLED_APPS`：`django.contrib.*`、`rest_framework`、`corsheaders`、`forum`
- `MIDDLEWARE`：`SecurityMiddleware`、`SessionMiddleware`、`CommonMiddleware`、`CsrfViewMiddleware`、`AuthenticationMiddleware`、`MessageMiddleware`、`XFrameOptionsMiddleware`、`CorsMiddleware`
- `ROOT_URLCONF`：`tieba_backend.urls`
- `TEMPLATES`：启用 `APP_DIRS`，模板目录由应用提供
- `STATICFILES_DIRS`：`BASE_DIR/static`
- `CORS_ALLOWED_ORIGINS`：`http://localhost:5173`、`http://127.0.0.1:5173`

**认证与权限**（开发环境）
- `REST_FRAMEWORK.DEFAULT_AUTHENTICATION_CLASSES`：`SessionAuthentication`、`BasicAuthentication`
- `REST_FRAMEWORK.DEFAULT_PERMISSION_CLASSES`：`AllowAny`
- `LOGIN_URL`：`/login/`

**数据库**
- 默认 `SQLite`：`'ENGINE': 'django.db.backends.sqlite3'`，`'NAME': BASE_DIR / 'db.sqlite3'`
- 生产可切换 MySQL（参考 Django 官方 `DATABASES` 配置）

## 4. 前端架构
- 模板层（Django 模板）：`forum/base.html` + 页面模板（`index.html`、`bar_detail.html`、`post_detail.html`、`post_form.html`、`login.html`、`register.html`）
- 静态样式：`static/css/`（统一布局、两栏网格、表单样式、移动端适配）
- 静态站点导出与演示：`tieba_backend/static_site/`（`bars/`、`posts/`、`index.html`）

## 5. 数据层设计
- 用户模型：继承 Django `auth_user`（使用内置认证体系）
- 业务模型（`forum/migrations/`）：
  - `Category(name, slug, created_at)`
  - `Bar(name, description, cover_url, followers, category(FK), created_at)`
  - `Post(title, content, author(FK User), bar(FK), created_at, updated_at, last_reply_at)`
  - `Comment(post(FK), author(FK), content, likes, parent(self FK, 可空), created_at)`
- 关系参见 E-R 图：`E-R图.png` 或 `tieba_backend/docs/ER.md`

## 6. 接口与视图
**页面路由**（`forum/web_urls.py`）
- `/` → `index`（首页：热门吧/热门帖聚合）
- `/bars/<bar_id>/` → `bar_detail`（吧详情，支持 `sort=new/hot`）
- `/posts/<post_id>/` → `post_detail`（帖子详情/评论）
- `/posts/new/` → `create_post`（发帖，登录态要求）
- `/login/`、`/logout/`、`/register/`（登录注册）

**API 路由**（`forum/urls.py` + DRF Router）
- `/api/categories` → `CategoryViewSet`
- `/api/bars` → `BarViewSet`
- `/api/posts` → `PostViewSet`
- `/api/register` → `register`

## 7. 运行与联调
- 启动后端：`python manage.py runserver 127.0.0.1:8000`
- 启动静态预览：`python -m http.server 8010`（工作目录指向项目根）
- 预览地址：`http://127.0.0.1:8010/index.html` 与 `http://127.0.0.1:8000/`
- 跨域联调：若前端开发端口为 `5173`，已允许跨域（见 `CORS_ALLOWED_ORIGINS`）

## 8. 安全与配置
- 开发环境：`DEBUG=True`，`ALLOWED_HOSTS=['*']`（仅限本机使用）
- 生产建议：
  - 设置强随机 `SECRET_KEY`，关闭 `DEBUG`
  - 严格配置 `ALLOWED_HOSTS`
  - 启用 `CSRF` 与会话安全（已默认开启）
  - 统一鉴权：生产可切换 `JWT + HTTPS`
  - 资源缓存与限流：前置 `Nginx` + 缓存策略

## 9. 静态导出与提交
- 管理命令：`forum/management/commands/export_html.py`（将模板数据导出为静态 HTML 便于演示与提交）
- 目录：导出结果位于 `tieba_backend/static_site/`（含 `bars/`、`posts/`、`index.html`）

## 10. 目录结构（简要）
```
D:\baidutieba
├── index.html / post_detail.html / edit.html / new.html
├── login.html / register.html / base.html / post_form.html
├── tieba_backend\
│   ├── forum\           # 业务模块（models / views / urls / templates）
│   ├── static_site\     # 静态演示页（bars / posts / index.html）
│   └── tieba_backend\   # Django 配置（settings/urls/wsgi/asgi）
├── E-R图.png / 功能思维导图.png / 思维导图.jpeg
└── 贴吧页面原型.html / 文档（*.md / *.html）
```

## 11. 部署建议
- 应用层：`gunicorn/uwsgi` + Django，或容器化（Docker）
- 接入层：`Nginx` 反向代理（静态资源与媒体分发）
- 数据层：MySQL 主从（备份与恢复），Redis 用于会话/缓存（可选）
- 观测：接入日志、APM 与健康检查；完善单元测试与接口契约

---
